// Служебные подписи в чате (какую метку выбрала модель, какая функция движка вызвана)
// нужны только нам при отладке. Пользователю они не говорят ничего полезного,
// поэтому по умолчанию выключены: включаются сборкой с VITE_SHOW_DEBUG=1.
export const SHOW_DEBUG = import.meta.env.VITE_SHOW_DEBUG === '1'
