# Обучение генеративного пояснителя (шаг 3). Только на машине с GPU, в прод не идёт.
# torch ставится отдельной командой с индекса CUDA — см. README, раздел «Генеративный пояснитель».
transformers>=4.46,<5
peft>=0.13
trl>=0.12
accelerate>=1.0
bitsandbytes>=0.45
datasets>=3.0
sentencepiece
